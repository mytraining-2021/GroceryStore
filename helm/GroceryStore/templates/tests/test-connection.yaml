apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "GroceryStore.fullname" . }}-test-connection"
  labels:
    {{- include "GroceryStore.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "GroceryStore.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
